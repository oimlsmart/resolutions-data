source "https://rubygems.org"

# The latest released edoxen (0.5.0) carries an older meeting schema
# that doesn't recognise identifier/localizations/minutes on Meeting.
# Pin to main until 0.6.x ships.
gem "edoxen", github: "edoxen/edoxen", branch: "main"
