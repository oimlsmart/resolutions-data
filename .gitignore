# OCR raw API cache (reproducible from PDFs via scripts/ocr/run.rb;
# the md/ outputs are tracked in git as the canonical OCR text)
reference-docs/ocr/raw/
reference-docs/ocr/text/

# Browser build artifacts (reproducible via `npm run build`)
browser/dist/
browser/.vite/
browser/.vite-ssg-temp/
browser/tsconfig.tsbuildinfo
browser/node_modules/

# Ruby
.bundle/
vendor/

# OS / editor
.DS_Store
*.tmp
*.bak
*.log
Gemfile.lock

# Root node_modules (scripts that need npm deps)
node_modules/
